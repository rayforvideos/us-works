# 미룬 리팩토링

지금 구조로 가기로 하면서 나중으로 미룬 것들을 모은다. 각 줄은 어떤 ADR이나 작업에서 나왔는지, 무엇이 충족되면 다시 볼지를 함께 적는다. 고장이나 문서와 코드의 불일치는 여기 적지 않고 `fix` 이슈로 만든다.

| 대상                 | 지금                                                               | 바꿀 방향                                                       | 다시 볼 조건                                                                          | 이슈 |
| -------------------- | ------------------------------------------------------------------ | --------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ---- |
| 작성 중 이탈 확인    | GNB 뒤로가기만 확인 모달로 막는다. 브라우저 뒤로가기는 그냥 나간다 | Navigation API로 히스토리 이동도 같은 모달로 막는다             | React 19.3 네이티브 기능 리팩토링에 착수할 때. 그 전에는 임시 장치를 넣지 않는다      | -    |
| React 19.3 네이티브  | 폼 상태는 TanStack Form, 전환 표시는 화면마다 직접 만든다          | 19.3에 들어온 기능으로 대체할 수 있는 자리를 골라 옮긴다        | 화면 작업이 끝나고 리팩토링에 착수할 때                                               | -    |
| 디자인 시스템 CLI    | Base UI 프리미티브를 직접 감싼다(ADR-0007, ADR-0015)               | shadcn/ui 같은 CLI로 공용 컴포넌트를 받아 쓴다                  | ADR-0007이 건 조건(오버레이 다수)은 이미 충족됐다. 토큰 동기화가 필요해지면 다시 본다 | -    |
| 날짜·시간 선택기     | 브라우저 기본 `datetime-local` 하나를 쓴다(ADR-0016)               | 달력과 시간 목록을 우리가 그리는 선택기로 바꾼다                | 디자인이 30분 단위를 요구로 확정하거나 예약 시각 화면이 셋 이상으로 늘면              | -    |
| 페이지 세그먼트 승격 | 소비자가 하나뿐인 모듈은 페이지 안에 둔다(ADR-0017)                | 두 번째 소비자가 생긴 모듈을 `features`나 `entities`로 올린다   | 같은 모듈을 두 화면이 쓰게 되거나 한 페이지의 모듈이 한눈에 안 들어올 때              | -    |
| 목록 방식            | 번호 페이지네이션을 쓴다(ADR-0013)                                 | 무한 스크롤로 바꾼다. Figma 노트는 둘 중 하나를 고르라고만 한다 | 한 페이지에 담기 힘들 만큼 목록이 길어지고 필터로도 좁혀지지 않을 때                  | -    |

## 쓰는 법

- 새 ADR은 Consequences의 마지막 줄에 "다시 볼 조건"을 한 줄 적고, 그 줄을 이 표가 모은다.
- 실제 작업은 `refactor` 라벨 이슈로 만들고 이슈 번호를 이 표에 적는다. 문서는 왜와 언제를, 이슈는 무엇을 맡는다.
- 조건이 충족되어 작업을 마치면 그 줄을 지운다. 결정이 바뀌었으면 새 ADR에 남기고 지운다.
