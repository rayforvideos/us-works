import { cva } from "class-variance-authority";

export const noticeClass = cva("flex flex-col items-center gap-4");

export const messageClass = cva("text-14-sb600 text-grey-500");

export const linkClass = cva("text-14-sb600 text-blue-green-90 underline");
