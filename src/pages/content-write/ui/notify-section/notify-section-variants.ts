import { cva } from "class-variance-authority";

export const notifySectionClass = cva("flex flex-col gap-6");

export const headerBoxClass = cva("flex flex-col gap-3");

export const headingBoxClass = cva("flex flex-col gap-1");

export const notifyHeadingClass = cva("text-16-sb600 text-blue-grey-300");

export const notifyHelperClass = cva("text-12-m500 text-grey-300");

export const dividerClass = cva("border-t border-grey-100");

export const sentNoticeClass = cva("text-12-m500 text-grey-300");

export const rowsClass = cva("flex flex-col gap-4");

export const titleBoxClass = cva("flex flex-col items-start gap-2");
